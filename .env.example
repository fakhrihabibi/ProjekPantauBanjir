# AWS S3 settings
S3_BUCKET=your-bucket-name
AWS_REGION=ap-southeast-1
# AWS credentials (use IAM role where possible; don't commit real keys)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Optional: other envs used by the app
NEXT_PUBLIC_BASE_URL=http://localhost:3000
